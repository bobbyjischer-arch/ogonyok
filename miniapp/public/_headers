# index.html никогда не кешируется: вебвью Telegram иначе держит старую
# версию до полной очистки кеша. Хешированные ассеты immutable — имя файла
# меняется на каждой сборке само.
/
  Cache-Control: no-store
/assets/*
  Cache-Control: public, max-age=31536000, immutable
