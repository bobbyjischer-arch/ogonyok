/**
 * Реэкспорт: сам расчёт переехал в `shared/`, потому что тот же огонёк теперь
 * считает и бот. Файл оставлен, чтобы экраны продолжали импортировать
 * `../lib/streak` и не знали, где физически лежит логика.
 */

export * from '../../../shared/streak';
